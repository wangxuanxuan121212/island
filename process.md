# Xuanyu Island · 过程记录

## 任务与边界

- 日期：2026-09-07。
- 全部新增代码、依赖、文档、测试与截图位于 `xuanyu-island/`，不修改父目录现有网站。
- 参考 Bruno Simon 的全屏 3D 驾驶式个人网站；直接从小车、树木、长椅所在的可交互场景开始，无画圈开场。
- 明亮卡通风格，展示端与编辑端独立。
- 依据已有事实展示，不补造经历、业务效果、准确率或投资收益。
- 自主推进实现、构建、浏览器验证及本地预览。

## 01 · 资料与现状检查

- 读取 `../src/data/portfolio.default.ts`、`portfolio.schema.ts` 和现有数据保存逻辑，确认已有 React 19、Three.js、Vite 和 Zod 技术栈。
- 读取 `../04_素材/01_经历资料库/经历主库.md`，与结构化数据交叉核验。
- 确认个人姓名王轩钰，英文 Xuanyu Wang，复旦经济学院资产评估硕士；6 段实习、AI 研究与自动化项目、网球协会会长经历可形成世界站点。
- 保留事实边界：AI 效能研究不写成实证提效；投资系统不写收益；候选人项目不公开任何可识别个人的数据；手机默认不显示。
- 工作区已有前端工程，故不使用仅针对空工作区的 web-dev skill。已完整读取 TRAE-browseruse 指南，后续用于参考访问与浏览器验证。
- 工作区没有 Git 元数据；所有新增内容通过独立目录隔离，不改已有文件。

## 02 · 实现方向

- 视觉：日光、浅蓝天空、青绿草地、珊瑚红小车、粉色树冠、白色建筑与暖黄点缀；3D 场景铺满视口，无装饰性外框。
- 信息：欢迎广场、职业街区、AI 实验室、复旦校园、网球公园、联系站。
- 交互：真实物理库负责车体与碰撞；键盘与移动端方向控制；站点导航、地图、项目详情、重置、静音和降级阅读。
- 数据：展示端读取已发布内容；编辑端维护草稿、校验、预览、发布、导入导出；所有本地操作与旧站存储隔离。

## 03 · 作者资料核验

- 浏览器访问 `https://bruno-simon.com/`，结合用户提供的截图确认小车、树木、长椅和可驾驶世界的视觉组织。
- 作者开源工程：`https://github.com/brunosimon/folio-2025`。README 列出了输入、物理前处理、物理、车体同步、视角、环境动画与渲染的执行顺序。
- 读取作者工程 `package.json`：`https://raw.githubusercontent.com/brunosimon/folio-2025/main/package.json`，确认 Three.js 与 Rapier 物理引擎。
- 读取作者教程 `https://threejs-journey.com/lessons/physics` 的公开部分，参考物理世界与显示模型分离、每帧同步的实现方式。
- 本项目原创重建程序化场景，未复制作者的模型、配乐或整站代码。

## 04 · 实现

- 独立 Vite + React + TypeScript 工程，依赖安装在本目录。
- Three.js 全屏正交场景：欢迎广场、职业街区、AI 实验室、复旦校园、网球公园、来信码头，以及可驾驶木桥。
- 原创几何模型：珊瑚色越野车、四个独立轮胎及胎纹、车顶行李、樱花树、长椅、路灯、建筑、网球场、邮筒、风车等。
- Rapier 射线车辆控制器负责四轮悬挂、发动机力、转向、刹车和接触；固定 60 Hz 物理更新，Three.js 同步位置与旋转。
- 键盘与触屏方向按钮、地图传送、站点内容、发现记录、复位、缩放和用户主动开启的合成音效。
- 整理 18 条公开内容：6 段实习、6 个项目、4 条教育/研究、2 条校园/兴趣；未公开手机号、内部文档、候选人原始信息。
- 展示入口 `/`，独立编辑入口 `/editor.html`；阅读入口 `/?view=reading`，草稿预览 `/?preview=1`。
- 编辑端支持个人资料、条目增删与重排、公开开关、指标、边界说明、外观、撤销重做、JSON 导入导出。
- 草稿与发布存储键独立，发布时过滤隐藏条目；跨标签页通过 storage 事件更新。
- 编辑器不是线上受保护后台；发布作用于当前浏览器，导出的 `site.json` 可用于静态重新构建，完整部署说明见 `README.md`。
- 自检修正：驾驶按钮使用稳定 DOM，避免高频状态刷新导致触屏长按被组件重新挂载打断。
- 字体下载进 `public/fonts/` 并保留 OFL 许可，不依赖访客访问 Google Fonts。

## 05 · 初步验证

- `npm run build` 首轮成功：TypeScript 检查与 Vite 生产构建通过。
- 物理 WASM 包体积较大，Vite 提示分块警告；Three.js / Rapier 已拆为动态加载块，阅读与编辑不加载物理引擎。
- 启动独立预览：`http://127.0.0.1:5186/`，日志 `artifacts/server.log`，PID `artifacts/server.pid`。
- 首轮浏览器实测结果与最终复测见下节，不将构建成功当作交互验证。

## 06 · 浏览器验证与修正

- 使用 TRAE-browseruse 访问本地网站，读取页面结构和控制台，并检查实际首屏截图。
- 首轮截图发现 748px 窄窗口使用移动排版但相机仍偏右；相机切换改为与 760px CSS 断点一致，修正小岛裁切。
- 使用 Playwright 在独立浏览器上下文中测试，避免修改用户真实浏览器的已保存内容。
- 首轮 8 项测试有 7 项通过。桌面地图的“前往职业街区”同时存在于弹窗和底层小地图，测试选择器歧义。
- 弹窗打开时将背景设为 `inert`，保持焦点圈定与 Escape 关闭；随后把测试点击范围限定在当前 dialog，消除底层同名控件的歧义。
- 增加全岛鸟瞰与返回驾驶视角按钮；调整键盘处理，避免点击普通导航按钮后无法继续用方向键驾驶。
- 增加手机横屏样式，确保 844×390 下首屏按钮和页脚可见。
- 使用 Prettier 格式化所有源码和样式，保留可维护的文件结构。

### 最终 E2E 结果

`npm run test:e2e`：**12 passed（57.3 秒）**。

1. 桌面 1440×900：画布非空、动态帧、真实驾驶位移、地图、职业内容和实验室。
2. 手机 390×844：同上，并确认无横向溢出。
3. 窄窗口 748×691：同上，并确认修正后的构图。
4. 长按方向按钮跨多次 HUD 更新持续生效，释放和复位正常。
5. 草稿不改变发布页；草稿预览可见修改；发布跨页同步且刷新后保留。
6. 无效邮箱与非法 JSON 被拒绝；隐藏条目不进入展示端；撤销正常。
7. 手机编辑端、条目编辑和完整阅读版无横向溢出。
8. WebGL 初始化失败时自动打开完整阅读版。
9. 倒车与长椅碰撞、全岛鸟瞰、校园/网球/码头访问及邮件链接。
10. 内容新增、重排、删除、JSON 下载和完整导入恢复。
11. 本地存储失败时显示错误，不虚报发布成功。
12. 小屏手机 360×640 与横屏 844×390 的首屏控件可用。

### 画布与图片证据

- 桌面首屏像素采样：849 组量化颜色，14,998 个珊瑚/粉色样本和 7,807 个绿色样本；不是空白画布。
- 桌面一次方向输入的位置从 `(1.000, 1.999)` 变为 `(1.215, 2.588)`，其他视口同样有实际位移；帧数增长且两次画布图像不同。
- 首屏、驾驶、地图、内容抽屉、所有区域、编辑器、阅读版和小屏截图保存在 `artifacts/`。
- 各视口检查记录：`artifacts/desktop-evidence.json`、`mobile-evidence.json`、`tablet-evidence.json`。
- 完整机器可读测试报告：`artifacts/test-report.json`。

## 07 · 生产包验证与交付

- 最终 `npm run build` 通过，产物位于 `dist/`。
- `node scripts/verify-build.mjs` 临时启动生产预览并验证实际构建产物，完成后关闭该临时服务。
- 生产画布 1440×900、23,780 个三角面、动画帧递增、Rapier 初始化成功。
- 生产页面无 page error，无请求失败；字体等资源均成功加载。
- 独立检查 `/editor.html`，确认未请求 Three.js 或 Rapier 资源块。
- 生产证据：`artifacts/production-desktop.png`、`artifacts/production-verification.json`。
- 浏览器参考资料与事实边界另存 `references/sources.md`；运行、编辑、发布、部署和按键说明存入 `README.md`。
- 保留本地开发预览 `http://127.0.0.1:5186/`，编辑端 `http://127.0.0.1:5186/editor.html`。
- 没有部署到互联网，也没有配置鉴权后端。浏览器内发布与线上更新的区别已在 README 明确说明。
- 已知非阻断提示：Rapier WASM 分块较大；第三方 Zod 注释有 Rollup 注解提示；Rapier 初始化器有弃用参数警告。构建成功，交互测试与生产检查无页面错误。没有为消除提示而隐藏控制台输出。

## 进度

- [x] 读取个人事实与现有工程。
- [x] 核验参考站与作者技术资料。
- [x] 3D 世界、驾驶与内容展示。
- [x] 独立编辑端与发布流程。
- [x] 构建与桌面/移动端浏览器验证。
- [x] 预览地址与使用文档。

## 08 · 驾驶与方位引导优化

- 本轮要求：优化小车移动、转弯、方位引导；仍只修改 `xuanyu-island/`。
- 读取现有引擎、道路模型、展示 UI、样式和测试；使用 frontend-design 保持明亮玩具小岛风格，使用 TRAE-browseruse 指南进行浏览器验证。
- 新增可重复测量脚本 `scripts/measure-driving.mjs`，优化前数据与截图保存至 `artifacts/driving/before*`。
- 优化前实测：直行松键约 1.1 秒后仍为 17 km/h；倒车速度达到 25 km/h；软件渲染测试中 1.3 秒仅约 9 帧，原有 50ms 帧时间截断会进一步降低物理推进速度。
- 静态检查：前后驱动力同上限、无换向刹停；方向键来源共用 Set，释放一个别名会打断另一个仍按下的按键；小地图只有位置圆点，没有朝向或路线；桥面仅 3.4m。
- 实施方向：渐进油门、分方向限速、松键制动、低速大转角与高速稳定性、固定物理步长加渲染插值、静态几何合批、桥面余量、沿道路寻路与车头方向提示。
- 导航与传送分开：可以选择目的地沿路线驾驶，同时保留直接前往入口。
- [x] 完成车辆与渲染优化。
- [x] 完成路径、地图与移动端引导。
- [x] 完成优化前后测量、浏览器回归和生产验证。

### 车辆控制

- 油门由瞬时固定驱动力改为渐进变化；前进上限约 26 km/h、倒车约 10 km/h。
- 松键主动制动，反向输入先刹停再换向；刹车时不继续施加发动机力。
- 低速最大转角约 47°，随速度上升收敛，松键快速回正；提升轮胎横向抓地、锁定车身俯仰/侧翻自由度，保留 Rapier 悬挂、碰撞和转向。
- 输入按来源管理，W / 方向键 / 触屏 / 按钮键盘操作互不错误释放；多指控制、窗口失焦、弹窗与鸟瞰的输入状态均有覆盖。
- 增加 D / R / P 档位、实时按压状态、车前深绿色朝向箭头；手机方向键扩大到 44×42px。
- 固定 60Hz 物理更新与显示插值分离；提高低帧率补帧上限，并在切回标签页时丢弃过期时间。
- 相机随车速适度前看，倒车时反向预留视野；复位时同步物理、显示、相机与导航状态。

### 方位和路线

- 引入 PathFinding.js A*，在 0.5m 网格上建立岛屿、木桥和障碍物的通行模型，禁止斜穿障碍角。
- 木桥从 3.4m 扩至 4.8m；模型与碰撞、寻路使用同一份桥梁数据，避免地图和实际道路不一致。
- 地面金色箭头沿路线布置，地图显示道路、路线与车头；大/小地图按与主相机一致的斜视方向投影。
- 地图默认“驾车导航”，不瞬移；另设“直接前往”模式保留原功能。导航栏显示目标、转弯、剩余路程、到达状态与取消入口。
- 偏离路线时重算；路径不可用时显示明确状态，不画跨水面的直线。
- 排查并修复桥头网格留距断开、矩形障碍过度膨胀造成的通路切断。校园停车点移至建筑外侧，实验室与校园两棵树移离通道。

### 运行时问题与证据

- 手机导航测试发现弹窗未关闭。使用 TRAE-debugger 记录原生输入、焦点、面板状态、A* 结果与页面异常，诊断会话为 `map-navigation-state`。
- 排除了重复触摸点击和焦点误触。实际原因：PathFinding.js `smoothenPath` 使用未声明变量 `lastValidCoord`，ESM 严格模式抛错，阻断后续关闭弹窗。
- 改为库自带 `compressPath`，保留 A* 路线形状、不执行有缺陷的平滑函数。修复后日志确认 A* 成功、无异常、panel 变为 null。
- 未清理诊断会话：待用户确认或叫停后处理。采集仅对 `127.0.0.1` 且显式带 `?debug=1` 的页面启用，普通预览无日志网络请求。

### 性能处理

- 同材质静态几何按岛屿合批，动画树木、旗帜、车轮保持独立；逐岛保留视锥裁剪，不将整个世界合成一个不可裁剪模型。
- 阴影贴图改为 1024×1024，保持卡通柔和投影，降低软件渲染和移动设备负担。
- 最终测量中普通驾驶场景约 146–157 次 draw call。软件渲染测试不能代表用户设备帧率，不承诺固定 FPS。

### 优化前后对比

数据文件为 `artifacts/driving/before.json` 和 `artifacts/driving/after.json`，脚本 `scripts/measure-driving.mjs` 可重跑。

| 场景 | 优化前 | 优化后 |
| --- | --- | --- |
| 直行后松键约 1.1 秒 | 17 km/h，继续滑行 | 0 km/h，档位 P |
| 同一倒车测量过程 | 25 km/h | 9 km/h |
| 左右输入 | 转向变化较小 | 本轮约 +0.83 / -0.87 弧度，释放后方向回正 |
| 目的地方位 | 只有站点/传送，无路线 | 地面路径、地图朝向、转向/距离/到达状态 |

### 验证结果

- `npm run test:e2e`：**20 项全部通过（约 2 分钟）**，包含原 12 项回归和 8 项驾驶专项。
- 专项包含：松键停车、先制动再反向、倒车限速、左右转向、回正、键盘别名与指针隔离、真实双指触控、弹窗/鸟瞰/失焦清理。
- 六个站点停车点到其他站点的 **30 组路线**全部验证可达，选择导航不改变车辆位置。
- 实际路试：只用 Playwright 键盘控制沿桥驶入网球公园，无传送；路线指示距离减少、最终 `arrived=true`。轨迹见 `artifacts/driving/verified-journey.json`，截图见 `arrival.png`。
- 桌面、390×844 手机、748×691 窄窗口、360×640 小屏与844×390 横屏保持可用；手机 HUD 与驾驶/内容入口不重叠。
- 已检查 `mobile-guidance.png`、`mobile-route-map.png`、`desktop-route-overview.png`、`arrival.png` 以及原有首屏/内容/编辑截图。
- TypeScript 和生产构建通过。生产包新增验证导航至复旦校园，生成 8 个路点、约 21m 路线；弹窗正常关闭，无页面错误或失败请求，编辑端仍未加载 3D 依赖。
- 继续保留预览 `http://127.0.0.1:5186/`；公开经历、编辑内容和父目录原站均未更改。

## 09 · 紧凑公园与观光车

### 需求与范围

- 用户反馈岛屿分散、驾驶容易被障碍物卡住，要求减少阻碍并继续精致化场景。
- 后续追加：完成前述优化后，将小车替换为更适配当前风格的精致车型。
- 仅修改 `xuanyu-island/`；公开个人经历、阅读版和编辑数据契约保持不变。

### 场景与驾驶

- 六个区域向中央收拢，改为一块连续椭圆陆地，移除窄桥通行限制。
- 6.4m 宽的公园步道连接主要区域，并增加外圈通路，草地也允许驾驶。
- 树木、建筑、长椅、花坛、球网不再创建阻挡车身的碰撞体；建筑与树木靠近时渐隐，避免遮住穿行中的车。
- 保留 Rapier 地面碰撞与四轮悬挂；加入低速/原地转向辅助和岸边安全留距。
- 寻路仍绕开主要建筑与树干；穿行到建筑内后可以从附近通行网格重新规划。
- 花坛、长椅和路牌尽量靠路边布置，补充屋顶太阳能板、建筑入口种植、海边木平台及成组树木。
- 路网与广场一次绘制到程序化地表纹理，消除交叉路面的深度闪烁。手机与桌面地点标签避开固定 HUD 和相互重叠。

### 新车型

- 用原创复古电动观光车替换越野车，轮距、轴距及轮胎半径不变，以沿用已验证的物理行为。
- 薄荷绿车身、瓷白顶棚与引擎盖、圆灯、弧形轮拱、白边轮毂、金属饰条、双座、方向盘、透明风挡和后置野餐篮。
- 大曲面保留圆润轮廓，小饰件减少细分；静态部分仍合批，四个车轮独立动画。
- 默认车色改为 `#6aada1`，已有浏览器中的草稿或已发布自定义车色不覆盖；编辑器发布换色有独立测试。

### 性能与验证

- 运行时采样确认软件渲染长帧会触发旧的 120ms 时间截断，表现为输入已收到但车辆推进偏慢，而不是输入丢失。
- 降低像素比上限、按帧耗时逐级降低画布分辨率、阴影限频刷新，并将物理补帧上限调整至 250ms，仍保持 1/60 固定步长。
- 中断后重跑原先失败的 5 项驾驶测试全部通过；新车桌面、手机及发布换色 3 项专项通过。
- TypeScript 与生产构建通过；最终 `npm run test:e2e` **27 项全部通过（3.7 分钟）**。
- `node scripts/verify-build.mjs` 通过：生产包无页面异常或请求失败，复旦校园路线可达，编辑端不加载 3D 依赖。
- 已检查桌面/手机车型正侧面、全岛鸟瞰、穿行建筑、手机导航与内容画面；对应截图位于 `artifacts/runabout/`、`artifacts/relaxed-driving/` 和 `artifacts/`。
- 调试采样仅在本机显式 `?driveDebug=1` 开启，普通访问不发调试请求。记录为 `debug-park-driving.md`，用户确认后清理。
